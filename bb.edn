{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([iho_ships.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'iho_ships.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
